# 项目未完成事项清单

更新时间：2026-05-23

## 当前结论

项目主链路已经基本闭环：教材解析、课程方案生成、教案生成、多场景测练、覆盖报告、课程方案/教案/试卷 DOCX 下载、课件 PPTX 下载、题目沉淀首版展示和演示材料均已具备。

当前剩余事项不是重做主功能，而是补齐题库查询接口、修正覆盖报告稳健性，并做最终演示校准。

## 未完成事项

### 1. 解析证据摘要真实数据接入

状态：已完成后端接口，前端已按最新字段适配

接口：

```http
GET /api/v1/parse-versions/{parse_version_id}/evidence-summary
```

用途：

- 支撑教材解析页“解析证据”区域展示 MinerU 解析能力。
- 让评委能看到解析版本、block 统计、资源统计、MinerU 参数和示例证据。

后端当前返回内容：

- 解析版本基础信息：`parse_version_id`、`strategy_code`、`mineru_model`、`parse_status`、`review_status`
- 规模统计：`volume.page_count`、`volume.block_count`、`volume.issue_count`
- block 类型统计：`block_type_counts`
- 多媒体/结构统计：`volume.image_block_count`、`volume.table_block_count`、`volume.equation_block_count`、`volume.asset_block_count`
- MinerU 参数摘要：`mineru_parameters`
- 示例证据：`sample_blocks`

前端当前状态：

- 已接入 `ParseEvidenceSummary` 最新字段。
- 已接入 `getParseEvidenceSummary(parseVersionId)` API wrapper。
- 教材解析页已增加“解析证据”区域。
- 接口失败或空数据时仍保留降级提示，不展示虚构统计。

验收标准：

- 项目工作台教材解析页可以展示真实 block 类型统计。
- 图片、表格、公式等资源统计来自后端真实数据。
- 示例证据能对应到真实页码和 block。
- 接口失败时页面不崩溃，有明确等待/失败提示。

### 2. 后端题库查询接口

状态：待后端实现

接口：

```http
GET /api/v1/question-items
```

用途：

- 支撑“题目沉淀/题库基础视图”从当前试卷题目展示升级为全批次题库查询。
- 展示系统具备题目沉淀能力，而不是只生成一次性试卷。

建议筛选参数：

- `generation_batch_id`
- `paper_result_id`
- `knowledge_point_id`
- `question_type`
- `difficulty_level`
- `scene_type`
- `page`
- `page_size`

建议返回内容：

- 题干
- 答案
- 解析
- 题型
- 难度
- 知识点
- 所属试卷
- 测练场景

前端当前状态：

- 已预留 `QuestionBankItem` 类型。
- 已预留 `listQuestionItems(params)` API wrapper。
- 测练页已增加“题目沉淀”视图。
- 当前首版基于选中试卷详情里的题目展示和筛选，不依赖未完成后端接口。

验收标准：

- 测练页可以按批次查看题目沉淀列表。
- 支持按题型、难度、知识点、测练场景筛选。
- 能区分 `unit_test`、`homework`、`final_exam` 等不同测练场景来源。
- 后端接口不可用时，当前试卷内题目展示仍可正常使用。

### 3. 前端题库真实接口切换

状态：等待 `question-items` 接口上线后处理

待做：

- 将测练页“题目沉淀”从当前试卷数据源切换或扩展为 `question-items` 全批次数据源。
- 保留接口失败降级提示，避免演示时空白或崩溃。

验收标准：

- 后端题库接口上线后，前端无需改页面结构，只需完成数据源接入和字段对齐。
- 缺字段、空数据、接口失败都有清晰状态。

### 4. 演示数据最终校准

状态：待题库接口和覆盖报告稳健性修复后再跑一轮

检查目标：

- 项目 `#1` / 批次 `#1` 至少包含 `unit_test` 和一个其他测练场景，如 `homework`。
- 覆盖报告包含课程方案、教案、试卷题目、课件页面四类覆盖矩阵。
- 解析版本能展示多种 block 类型，最好包含图片、表格或公式。
- 课程方案 DOCX、教案 DOCX、试卷 DOCX、课件 PPTX 均可下载。
- 题目沉淀能展示多场景题目来源。

参考文档：

- `docs/比赛演示脚本.md`
- `docs/演示数据检查清单.md`
- `docs/后端赛题评分补齐待办.md`

验收标准：

- 按演示脚本完整走一遍，所有关键点击都有数据。
- 评委视角可以理解“解析证据、生成链路、多场景测练、覆盖追踪、文档导出、题目沉淀”六个亮点。

## 已完成但需要保留回归验证的能力

- 多场景测练生成。
- 覆盖报告纳入 `question_item` 和 `courseware_slide`。
- 课程方案 DOCX 导出/下载。
- 教案 DOCX 导出/下载。
- 试卷 DOCX 导出/下载。
- 课件 PPTX 下载。
- 教材解析页解析证据真实数据展示。
- 测练页题目沉淀首版展示。
- 比赛演示脚本。
- 演示数据检查清单。

## 不在本轮范围内

- 学生端答题。
- 自动判分。
- 完整题库管理，如收藏、标签、智能组卷。
- 完整审计视图。
- 重新设计主生成链路。
